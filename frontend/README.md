# HelpDesk frontend

SvelteKit server-rendered frontend and BFF for the HelpDesk services. Backend origins and bearer tokens remain in server-only modules under `src/lib/server/api/`.

Use the repository-pinned pnpm version and run commands from the repository root. See [`../README.md`](../README.md) for bootstrap, local configuration, development, validation, and OpenAPI workflows.
