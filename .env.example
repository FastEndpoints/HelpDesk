# Local Podman Compose credentials only. Copy to .env and replace both values.
MONGO_ROOT_USERNAME=helpdesk_local_admin
MONGO_ROOT_PASSWORD=replace-with-a-long-random-local-password
# Optional host-port override when 27017 is already in use.
MONGO_PORT=27017
